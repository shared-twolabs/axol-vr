{
  "name": "almond-axol-vr",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.{ts,tsx,json,css}": "prettier --write"
  }
}
